import * as React from 'react';
var FilterDropdownMenuWrapper = function (props) { return (React.createElement("div", { className: props.className, onClick: props.onClick }, props.children)); };
export default FilterDropdownMenuWrapper;
