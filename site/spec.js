import React from 'react';

export default class Spec extends React.Component {
  constructor(props) {
    super(props);
    this.state = {};
  }

  render() {
    return (
      <div className="app">
        Spec
      </div>
    );
  }
}
