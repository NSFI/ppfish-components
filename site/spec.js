import React from 'react';
import Layout from './layout';

export default class Spec extends React.Component {
  constructor(props) {
    super(props);
  }

  render() {
    return (
      <Layout>
        <div>设计语言</div>
      </Layout>
    );
  }
}
